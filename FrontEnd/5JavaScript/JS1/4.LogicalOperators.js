// A "good string" is a string that starts with letter 'a' and has a length > 3. wap to find if a string is good or not.

let randomWord = 'braij';
if(randomWord[0]==='a' && randomWord.length > 3){
    console.log(`It's a good string`)
}else{
    console.log(`Not a good string`)
}

let num = 12;
if((num%3===0)&&((num+1==15) || (num-1 ==11))){
    console.log(`safe`)
}else {
    console.log(`unsafe`)
}


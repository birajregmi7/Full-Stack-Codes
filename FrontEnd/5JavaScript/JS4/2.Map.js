//map function 
const array = [1,2,3,4]
const newArr = array.map((el)=>{ //el is callback function 
    return (el*2);
})
console.log(newArr)

//map is similar to for each but in map creates new array of same size as orginal and stores the output 

